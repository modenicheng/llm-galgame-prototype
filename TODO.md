# TODO — 大方向路线图

> 来源：2026-09 讨论共识。条目保持**方向级**，不展开具体实现；
> 「参考方向」仅作提示，落地前先对照 `docs/status.md` 的现状。

## 讨论共识摘要

- 当前优先级：**优化语音合成 & 长期记忆系统**。
- 长期记忆的定位：**LLM 编剧的上下文管理系统**——用于伏笔回收、草蛇灰线，
  不是聊天记录式记忆。
- 长期记忆与多周目是一致的：做多周目同样需要更好的上下文管理，需要前几周目
  的数据。
- 两条底线：**不能吞设定**（世界观/人物设定不可在上下文管理中丢失）；
  **决策结果要一致**（同一状态下编剧决策不漂移）。
- 多周目体验的前置是**存档系统**；之后还有**成就/收集系统**。
- 动态资源生成：音频可以走 **LLM + 规则 + 合成器** 驱动；美术需要 **API**。
  （或者：如何提供足量的资源。）

## 优先级顺序

```text
近期：  1 长期记忆/上下文管理   2 语音合成优化
中期：  3 存档系统（前置） → 4 多周目体验
后续：  5 成就/收集系统
探索：  6 动态资源生成（音频可先行，美术依赖 API）
```

---

## 1. 长期记忆系统 —— LLM 编剧的上下文管理（核心）

- 现状：NarrativeDirector 第 1–3 步已上线（记忆过去 + 规划未来，
  threads/setups/anchors + 导演计划），但粒度是**单会话/单周目**。
- 方向：
  - 伏笔回收（setup → payoff）的可靠性与时机控制、草蛇灰线的长跨度铺垫。
  - **不能吞设定**：静态设定（世界观/人物 canon）与动态叙事事实分层管理，
    任何上下文压缩/摘要不得丢失设定。
  - **决策结果一致**：同一记忆状态下，编剧决策（走向/回收/揭示）应可复现。
  - 跨会话、跨周目的记忆分层。
- 参考方向：设定库（canon，只增不改）与叙事记忆（可演化）双库；检索增强
  （spec 第 4 步预留位：belief / embedding / SQLite）；一致性校验/冲突检测；
  记忆压缩的分层摘要。
- **已立项（2026-09-06）**：facts / beliefs / lessons / 异步审计的最小闭环与
  实施分期，见
  `docs/superpowers/specs/2026-09-06-narrative-memory-audit-design.md`
  （来源：`docs/novel-skill/` 可复用条目分析）。

## 2. 语音合成优化

- 现状：DashScope CosyVoice + PCM 流式 + IndexedDB 缓存 + 播放水位调度已跑通。
- 方向：
  - 质量：情感/韵律与剧情情绪、角色状态联动。
  - 延迟：首包时间、缓冲水位参数、合成并发与预取策略。
  - 成本：缓存命中率、相同内容复用。
  - 覆盖面：旁白配音策略（当前旁白不配音）、多角色音色扩展。
- 参考方向：情绪 → 合成参数/音色风格映射（performance-compiler 已是挂载点）；
  voices.yaml 音色体系扩展。

## 3. 存档系统（多周目的前置）

- 现状：`SessionStorePort` 只有 append/saveSnapshot，无 load/resume——重启即
  失忆，是当前最大的持久化缺口（见 status.md）。
- 方向：
  - 会话恢复闭环（load/resume：事件日志 + 叙事记忆 + 导演计划 + 视觉状态）。
  - 存档槽位、任意进度存/读。
- 注意：存档不只是事件回放——长线记忆、导演计划、VisualState 都需要一起快照，
  否则恢复后「吞设定」。恢复流程按「接管体检」原型设计：重放 committed
  events 重建长线记忆，全部就绪后才放行首次生成（见同 spec §12 Phase C）。

## 4. 多周目体验

- 依赖：存档系统（3）+ 跨周目上下文管理（1）。
- 方向：
  - 前周目数据进入新周目上下文（NG+ 式继承：世界状态、角色关系、已揭示真相）。
  - 周目间的伏笔/设定延续与差异（二周目视角）。
  - 跨周目决策一致性：同一选择链在不同周目得到一致结果，除非剧情机制显式改变。
- 参考方向：周目层元记忆（meta-memory，独立于 `sessions/<id>/` 的单会话布局）；
  周目编号进入 brief/计划上下文。

## 5. 成就 / 收集系统

- 依赖：存档 + 周目数据作为统计来源。
- 方向：结局图鉴、CG/立绘/音频收集、事件或 flag 驱动的成就触发。
- 参考方向：资源目录（resources.yaml）作为收集品登记表；flag/成就判定挂在
  committed events 或 StoryState 上，与叙事记忆同源。叙事记忆强化 spec 的
  终局报告（伏笔回收率/线程终态）是现成的结算数据源。

## 6. 动态资源生成（美术 & 音频）

- 音频（可行，先做）：**LLM + 规则 + 合成器** 驱动。现有 audio intent-planner +
  TTS 管线是雏形；可扩展到程序化 BGM/SE（规则/合成器生成），LLM 只产意图参数。
- 美术（需要 API）：图像生成 API 接入——生成 → 质检 → 注册进 resources.yaml
  的管线；AssetResolver 架构已为「资源来源无关」预留（§60），DSL 侧无需改动。
- 备选思路：不动态生成，而是解决「如何提供足量的资源」（素材库扩充 +
  catalog 纪律，复用 §71 资源选择原则）。
