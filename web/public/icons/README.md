# Icons

监控后台状态栏使用的图标，来自 [Lucide](https://lucide.dev)
（`lucide-static` v0.544.0，ISC License — https://lucide.dev/license）。

通过 CSS `mask` + `background-color: currentColor` 染色
（见 `web/src/monitor/monitor.css` 的 `.ico-*` 规则），不加运行时依赖。
